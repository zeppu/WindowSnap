<UserControl x:Class="Overlay.Views.InterfaceConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Overlay.Views"
             xmlns:viewModels="clr-namespace:Overlay.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="182.951" d:DesignWidth="350.656"
             d:DataContext="{d:DesignInstance viewModels:InterfaceConfigurationViewModel }"
             >
    <Grid Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
        <StackPanel Margin="2">
            <StackPanel.Resources>
                <Style TargetType="CheckBox">
                    <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
                </Style>
            </StackPanel.Resources>
            <CheckBox Margin="2,4"  IsChecked="{Binding Path=Autostart}"><TextBlock Text="Run Snapinator at Windows Startup (current user only)" /></CheckBox>
            <CheckBox Margin="2,4"  IsChecked="{Binding Path=CheckForUpdates}"><TextBlock Text="Check for updates" /></CheckBox>
        </StackPanel>
    </Grid>
</UserControl>

